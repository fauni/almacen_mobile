import 'package:flutter/material.dart';

class RecepcionComprasScreen extends StatelessWidget {
  const RecepcionComprasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Pendientes de aprobación'),);
  }
}